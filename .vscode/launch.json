{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Windows API Program",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/BresenhamCircleEff.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "Build C++ (g++)"
        }
    ]
}
